#include <bits/stdc++.h>

using namespace std;


// To sort multiple string according totheir size ( O(NlogN) )
bool cmp (string s1, string s2) {
    return s1.size() < s2.size();
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    return 0;
}